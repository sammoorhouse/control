# control

Control staffing and allocation CLI.

## Setup

```bash
python3 -m venv .venv
.venv/bin/pip install -e .
```

## Run

```bash
source .venv/bin/activate
control --help
```

Or without activation:

```bash
.venv/bin/control --help
```

## Tests

```bash
.venv/bin/pytest
```
